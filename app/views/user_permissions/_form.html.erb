<%= form_with(model: user_permission) do |form| %>
  <% if user_permission.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_permission.errors.count, "error") %> prohibited this user_permission from being saved:</h2>

      <ul>
        <% user_permission.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.number_field :user_id, value: @user.id, style: "display: none"%>
    <%= form.label @user.first_name %><br><br>
  </div>

  <div>
    <%= form.label :permission_id, style: "display: block" %>
    <%= form.collection_select :permission_id, @permissions,:id,:description %>
  </div><br><br>

  <div>
    <%= form.submit %>
  </div>
<% end %>
